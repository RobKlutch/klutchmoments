# Application (frontend + backend) environment variables
# Configure these in Vercel (or your hosting provider) and in a local .env file for the web app.

# Replicate inference
REPLICATE_API_TOKEN=your_replicate_token
REPLICATE_YOLO_MODEL=owner/model:version

# Supabase project
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=public-anon-key
SUPABASE_SERVICE_ROLE_KEY=service-role-key
